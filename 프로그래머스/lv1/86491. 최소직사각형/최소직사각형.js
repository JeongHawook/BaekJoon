function solution(sizes) {
    var answer = 0;
    let max = 0;
    for(let i ; i<sizes.length;i++){
       
       max=Math.max(size[i][0], size[i][1])      
    
    }
   
    console.log([...sizes[1]])
    
    
answer = Math.max(...sizes[1])*Math.max(...sizes[0])
    
    
    return answer;
}